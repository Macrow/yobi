#slides
  .slides_container
    - (1..5).to_a.each do |i|
      - slide = Settings.slides.send("slide#{i}")
      = link_to image_tag(slide.image, :size => "470x220", :alt => slide.title), slide.url, :title => slide.title, :target => "_blank"
= include_slide_js

