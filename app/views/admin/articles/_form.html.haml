.form
  = form_for [:admin, @article] do |f|
    .field
      = f.label :title
      = f.text_field :title
    .field
      = f.label :content
      = f.text_area :content, :id => "kindeditor_id"
      #image_upload
    .actions
      = f.submit
  = uploadify_and_kindeditor_js

