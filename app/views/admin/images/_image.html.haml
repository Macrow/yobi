= div_for image do
  = link_to image_tag(image.image.thumb.url, :size => "120x120"), image.image.url, :rel => "lightbox-mygallery", :title => image.image.to_s
  .center= link_to '删除图片', [:admin, @product, image], :confirm => 'Are you sure?', :method => :delete, :remote => true, :class => "pill negative button"

